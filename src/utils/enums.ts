export enum ListingType {
  cover = "cover",
  original = "original",
}

export enum Role {
  drums = "drums",
  guitar = "guitar",
  bass = "bass",
  piano = "piano",
  keyboard = "keyboard",
  vocals = "vocals",
  mix = "mix",
  mastering = "mastering",
}

export enum Genre {
  rock = "rock",
  pop = "pop",
}
